pub fn init_ps2() {
    // Initialize PS/2 keyboard and mouse with IRQ handlers
}

pub fn init_usb() {
    // Initialize USB stack, keyboard and mouse
}

// IRQ handlers, scancode decoding, mouse button handling to be implemented
