diff --git a/boot/bios/installer_stage2.S b/boot/bios/installer_stage2.S
--- a/boot/bios/installer_stage2.S
+++ b/boot/bios/installer_stage2.S
@@ -1,5 +1,5 @@
- */ sti mov %dl, boot_drive movb $0, selected_drive mov $0x0003, %ax int $0x10 main_menu: call clear_screen mov $title_text, %si call print_string mov $subtitle_text, %si call print_string call print_drive_summary mov $menu_text, %si call print_string call read_key cmp $'1', %al je deploy_installer_media cmp $'2', %al je select_target_disk cmp $'3', %al je apply_bootloader_menu cmp $'4', %al je show_disk_info cmp $'5', %al je show_shell cmp $'6', %al je reboot_system cmp $27, %al je halt_system jmp main_menu deploy_installer_media: call clear_screen mov $install_intro_text, %si call print_string call print_drive_summary movb selected_drive, %al test %al, %al jz no_target_disk cmpb boot_drive, %al je no_target_disk mov $confirm_text, %si call print_string call read_key cmp $'y', %al je do_clone cmp $'Y', %al je do_clone jmp main_menu do_clone: call clear_screen mov $copy_begin_text, %si call print_string call print_drive_summary call clone_system_image jc clone_failed mov $copy_success_text, %si call print_string call wait_for_keypress jmp main_menu clone_failed: mov $copy_failed_text, %si call print_string call wait_for_keypress jmp main_menu no_target_disk: mov $no_target_text, %si call print_string call wait_for_keypress jmp main_menu apply_bootloader_menu: call clear_screen mov $bootloader_intro_text, %si call print_string call print_drive_summary movb selected_drive, %al test %al, %al jz no_target_disk cmpb boot_drive, %al je no_target_disk mov $bootloader_confirm_text, %si call print_string call read_key cmp $'y', %al je do_apply_bootloader cmp $'Y', %al je do_apply_bootloader jmp main_menu do_apply_bootloader: call clear_screen mov $bootloader_begin_text, %si call print_string call print_drive_summary call apply_bootloader_to_target jc bootloader_failed mov $bootloader_success_text, %si call print_string call wait_for_keypress jmp main_menu bootloader_failed: mov $bootloader_failed_text, %si call print_string call wait_for_keypress jmp main_menu select_target_disk: call select_next_target call clear_screen jc select_target_none mov $target_updated_text, %si call print_string call print_drive_summary call wait_for_keypress jmp main_menu select_target_none: mov $target_missing_text, %si call print_string call wait_for_keypress jmp main_menu show_disk_info: call clear_screen mov $disk_info_text, %si call print_string call print_drive_summary mov $disk_info_hint_text, %si call print_string call wait_for_keypress jmp main_menu show_shell: call clear_screen mov $shell_text, %si call print_string shell_loop: mov $prompt_text, %si call print_string call read_key cmp $'h', %al je shell_help cmp $'i', %al je show_disk_info cmp $'n', %al je shell_next_disk cmp $'b', %al je apply_bootloader_menu cmp $'d', %al je deploy_installer_media cmp $'r', %al je reboot_system cmp $'q', %al je main_menu mov $unknown_text, %si call print_string jmp shell_loop shell_help: mov $shell_help_text, %si call print_string jmp shell_loop shell_next_disk: call select_next_target jc shell_no_disk mov $target_updated_text, %si call print_string jmp shell_loop shell_no_disk: mov $target_missing_text, %si call print_string jmp shell_loop clear_screen: push %ax push %bx push %cx push %dx mov $0x0600, %ax mov $0x0700, %bx mov $0x0000, %cx mov $0x184F, %dx int $0x10 mov $0x0200, %ax xor %bx, %bx xor %dx, %dx int $0x10 pop %dx pop %cx pop %bx pop %ax ret read_key: mov $0x0000, %ax int $0x16 ret wait_for_keypress: mov $press_key_text, %si call print_string call read_key ret print_string: push %ax push %bx .print_loop: lodsb test %al, %al jz .print_done mov $0x0E, %ah mov $0x0007, %bx int $0x10 jmp .print_loop .print_done: pop %bx pop %ax ret print_char: push %ax push %bx mov $0x0E, %ah mov $0x0007, %bx int $0x10 pop %bx pop %ax ret print_hex_nibble: and $0x0F, %al cmp $10, %al jb .digit add $'A' - 10, %al jmp print_char .digit: add $'0', %al jmp print_char print_hex_byte: push %ax mov %al, %ah shr $4, %al call print_hex_nibble mov %ah, %al call print_hex_nibble pop %ax ret print_drive_id: push %ax mov $'0', %al call print_char mov $'x', %al call print_char pop %ax call print_hex_byte ret print_drive_line: call print_string movb (%bx), %al test %al, %al jz .none call print_drive_id mov $crlf_text, %si call print_string ret .none: mov $none_text, %si call print_string ret print_drive_summary: push %bx mov $boot_drive_text, %si mov $boot_drive, %bx call print_drive_line mov $target_drive_text, %si mov $selected_drive, %bx call print_drive_line pop %bx ret probe_drive: push %ax push %bx push %cx push %dx mov $0x55AA, %bx mov $0x4100, %ax int $0x13 jc probe_drive_fail cmp $0xAA55, %bx jne probe_drive_fail test $0x0001, %cx jz probe_drive_fail clc jmp probe_drive_done probe_drive_fail: stc probe_drive_done: pop %dx pop %cx pop %bx pop %ax ret find_default_target: movb $0, selected_drive movb $0x80, %bl find_default_target_loop: cmpb $0x88, %bl jae find_default_target_done cmpb boot_drive, %bl je find_default_target_next movb %bl, %dl call probe_drive jc find_default_target_next movb %bl, selected_drive ret find_default_target_next: inc %bl jmp find_default_target_loop find_default_target_done: ret select_next_target: push %ax push %bx push %cx push %dx movb selected_drive, %bl cmpb $0x80, %bl jae select_next_target_cycle movb $0x7F, %bl select_next_target_cycle: movb $8, %cl select_next_target_try: inc %bl cmpb $0x88, %bl jb select_next_target_check movb $0x80, %bl select_next_target_check: cmpb boot_drive, %bl je select_next_target_continue movb %bl, %dl call probe_drive jc select_next_target_continue movb %bl, selected_drive clc jmp select_next_target_done select_next_target_continue: dec %cl jnz select_next_target_try movb $0, selected_drive stc select_next_target_done: pop %dx pop %cx pop %bx pop %ax ret clone_system_image: push %ax push %bx push %cx push %dx push %si movb boot_drive, %dl call probe_drive jc clone_system_image_fail movb selected_drive, %dl call probe_drive jc clone_system_image_fail movl system_image_start_lba, %eax testl %eax, %eax jz clone_system_image_fail movl %eax, current_lba movl $0, current_lba + 4 movl $0, target_lba movl $0, target_lba + 4 movl system_image_total_sectors, %eax testl %eax, %eax jz clone_system_image_fail movl %eax, remaining_sectors clone_system_image_copy_loop: movl remaining_sectors, %eax testl %eax, %eax jz clone_system_image_success cmpl $COPY_CHUNK_SECTORS, %eax jbe clone_system_image_use_remaining movw $COPY_CHUNK_SECTORS, dap_sector_count jmp clone_system_image_read_chunk clone_system_image_use_remaining: movw %ax, dap_sector_count clone_system_image_read_chunk: movl current_lba, %eax movl %eax, dap_lba movl current_lba + 4, %eax movl %eax, dap_lba + 4 movb boot_drive, %dl mov $dap, %si mov $0x4200, %ax int $0x13 jc clone_system_image_fail movl target_lba, %eax movl %eax, dap_lba movl target_lba + 4, %eax movl %eax, dap_lba + 4 movb selected_drive, %dl mov $dap, %si mov $0x4300, %ax int $0x13 jc clone_system_image_fail movw dap_sector_count, %ax addl %eax, current_lba adcl $0, current_lba + 4 addl %eax, target_lba adcl $0, target_lba + 4 subl %eax, remaining_sectors mov $'.', %al call print_char jmp clone_system_image_copy_loop clone_system_image_success: mov $crlf_text, %si call print_string clc jmp clone_system_image_done clone_system_image_fail: stc clone_system_image_done: pop %si pop %dx pop %cx pop %bx pop %ax ret apply_bootloader_to_target: push %ax push %bx push %cx push %dx push %si movb boot_drive, %dl call probe_drive jc apply_bootloader_fail movb selected_drive, %dl call probe_drive jc apply_bootloader_fail movl system_image_start_lba, %eax testl %eax, %eax jz apply_bootloader_fail movl %eax, current_lba movl $0, current_lba + 4 movl $0, target_lba movl $0, target_lba + 4 movl $BOOT_AREA_SECTORS, remaining_sectors apply_bootloader_copy_loop: movl remaining_sectors, %eax testl %eax, %eax jz apply_bootloader_success cmpl $COPY_CHUNK_SECTORS, %eax jbe apply_bootloader_use_remaining movw $COPY_CHUNK_SECTORS, dap_sector_count jmp apply_bootloader_read_chunk apply_bootloader_use_remaining: movw %ax, dap_sector_count apply_bootloader_read_chunk: movl current_lba, %eax movl %eax, dap_lba movl current_lba + 4, %eax movl %eax, dap_lba + 4 movb boot_drive, %dl mov $dap, %si mov $0x4200, %ax int $0x13 jc apply_bootloader_fail movl target_lba, %eax movl %eax, dap_lba movl target_lba + 4, %eax movl %eax, dap_lba + 4 movb selected_drive, %dl mov $dap, %si mov $0x4300, %ax int $0x13 jc apply_bootloader_fail movw dap_sector_count, %ax addl %eax, current_lba adcl $0, current_lba + 4 addl %eax, target_lba adcl $0, target_lba + 4 subl %eax, remaining_sectors jmp apply_bootloader_copy_loop apply_bootloader_success: clc jmp apply_bootloader_done apply_bootloader_fail: stc apply_bootloader_done: pop %si pop %dx pop %cx pop %bx pop %ax ret reboot_system: movw $0x1234, 0x472 ljmp $0xFFFF, $0x0000 halt_system: cli .halt: hlt jmp .halt title_text: .asciz "OS8 DOS System Installer\r\n" subtitle_text: .asciz "16-bit real mode fallback path\r\nDeploys the bundled regular OS system image to another BIOS disk\r\n\r\n" menu_text: .asciz " 1.
+ */ sti mov %dl, boot_drive movb $0, selected_drive call find_default_target mov $0x0003, %ax int $0x10 main_menu: call clear_screen mov $title_text, %si call print_string mov $subtitle_text, %si call print_string call print_drive_summary mov $menu_text, %si call print_string call read_key cmp $'1', %al je deploy_installer_media cmp $'2', %al je select_target_disk cmp $'3', %al je apply_bootloader_menu cmp $'4', %al je show_disk_info cmp $'5', %al je show_shell cmp $'6', %al je reboot_system cmp $27, %al je halt_system jmp main_menu deploy_installer_media: call clear_screen mov $install_intro_text, %si call print_string call print_drive_summary movb selected_drive, %al test %al, %al jz no_target_disk cmpb boot_drive, %al je no_target_disk mov $confirm_text, %si call print_string call read_key cmp $'y', %al je do_clone cmp $'Y', %al je do_clone jmp main_menu do_clone: call clear_screen mov $copy_begin_text, %si call print_string call print_drive_summary call clone_system_image jc clone_failed mov $copy_success_text, %si call print_string call wait_for_keypress jmp main_menu clone_failed: mov $copy_failed_text, %si call print_string call wait_for_keypress jmp main_menu no_target_disk: mov $no_target_text, %si call print_string call wait_for_keypress jmp main_menu apply_bootloader_menu: call clear_screen mov $bootloader_intro_text, %si call print_string call print_drive_summary movb selected_drive, %al test %al, %al jz no_target_disk cmpb boot_drive, %al je no_target_disk mov $bootloader_confirm_text, %si call print_string call read_key cmp $'y', %al je do_apply_bootloader cmp $'Y', %al je do_apply_bootloader jmp main_menu do_apply_bootloader: call clear_screen mov $bootloader_begin_text, %si call print_string call print_drive_summary call apply_bootloader_to_target jc bootloader_failed mov $bootloader_success_text, %si call print_string call wait_for_keypress jmp main_menu bootloader_failed: mov $bootloader_failed_text, %si call print_string call wait_for_keypress jmp main_menu select_target_disk: call select_next_target call clear_screen jc select_target_none mov $target_updated_text, %si call print_string call print_drive_summary call wait_for_keypress jmp main_menu select_target_none: mov $target_missing_text, %si call print_string call wait_for_keypress jmp main_menu show_disk_info: call clear_screen mov $disk_info_text, %si call print_string call print_drive_summary mov $disk_info_hint_text, %si call print_string call wait_for_keypress jmp main_menu show_shell: call clear_screen mov $shell_text, %si call print_string shell_loop: mov $prompt_text, %si call print_string call read_key cmp $'h', %al je shell_help cmp $'i', %al je show_disk_info cmp $'n', %al je shell_next_disk cmp $'b', %al je apply_bootloader_menu cmp $'d', %al je deploy_installer_media cmp $'r', %al je reboot_system cmp $'q', %al je main_menu mov $unknown_text, %si call print_string jmp shell_loop shell_help: mov $shell_help_text, %si call print_string jmp shell_loop shell_next_disk: call select_next_target jc shell_no_disk mov $target_updated_text, %si call print_string jmp shell_loop shell_no_disk: mov $target_missing_text, %si call print_string jmp shell_loop clear_screen: push %ax push %bx push %cx push %dx mov $0x0600, %ax mov $0x0700, %bx mov $0x0000, %cx mov $0x184F, %dx int $0x10 mov $0x0200, %ax xor %bx, %bx xor %dx, %dx int $0x10 pop %dx pop %cx pop %bx pop %ax ret read_key: mov $0x0000, %ax int $0x16 ret wait_for_keypress: mov $press_key_text, %si call print_string call read_key ret print_string: push %ax push %bx .print_loop: lodsb test %al, %al jz .print_done mov $0x0E, %ah mov $0x0007, %bx int $0x10 jmp .print_loop .print_done: pop %bx pop %ax ret print_char: push %ax push %bx mov $0x0E, %ah mov $0x0007, %bx int $0x10 pop %bx pop %ax ret print_hex_nibble: and $0x0F, %al cmp $10, %al jb .digit add $'A' - 10, %al jmp print_char .digit: add $'0', %al jmp print_char print_hex_byte: push %ax mov %al, %ah shr $4, %al call print_hex_nibble mov %ah, %al call print_hex_nibble pop %ax ret print_drive_id: push %ax mov $'0', %al call print_char mov $'x', %al call print_char pop %ax call print_hex_byte ret print_drive_line: call print_string movb (%bx), %al test %al, %al jz .none call print_drive_id mov $crlf_text, %si call print_string ret .none: mov $none_text, %si call print_string ret print_drive_summary: push %bx mov $boot_drive_text, %si mov $boot_drive, %bx call print_drive_line mov $target_drive_text, %si mov $selected_drive, %bx call print_drive_line pop %bx ret probe_drive: push %ax push %bx push %cx push %dx mov $0x55AA, %bx mov $0x4100, %ax int $0x13 jc probe_drive_fail cmp $0xAA55, %bx jne probe_drive_fail test $0x0001, %cx jz probe_drive_fail clc jmp probe_drive_done probe_drive_fail: stc probe_drive_done: pop %dx pop %cx pop %bx pop %ax ret find_default_target: movb $0, selected_drive movb $0x80, %bl find_default_target_loop: cmpb $0x88, %bl jae find_default_target_done cmpb boot_drive, %bl je find_default_target_next movb %bl, %dl call probe_drive jc find_default_target_next movb %bl, selected_drive ret find_default_target_next: inc %bl jmp find_default_target_loop find_default_target_done: ret select_next_target: push %ax push %bx push %cx push %dx movb selected_drive, %bl cmpb $0x80, %bl jae select_next_target_cycle movb $0x7F, %bl select_next_target_cycle: movb $8, %cl select_next_target_try: inc %bl cmpb $0x88, %bl jb select_next_target_check movb $0x80, %bl select_next_target_check: cmpb boot_drive, %bl je select_next_target_continue movb %bl, %dl call probe_drive jc select_next_target_continue movb %bl, selected_drive clc jmp select_next_target_done select_next_target_continue: dec %cl jnz select_next_target_try movb $0, selected_drive stc select_next_target_done: pop %dx pop %cx pop %bx pop %ax ret clone_system_image: push %ax push %bx push %cx push %dx push %si movb boot_drive, %dl call probe_drive jc clone_system_image_fail movb selected_drive, %dl call probe_drive jc clone_system_image_fail movl system_image_start_lba, %eax testl %eax, %eax jz clone_system_image_fail movl %eax, current_lba movl $0, current_lba + 4 movl $0, target_lba movl $0, target_lba + 4 movl system_image_total_sectors, %eax testl %eax, %eax jz clone_system_image_fail movl %eax, remaining_sectors clone_system_image_copy_loop: movl remaining_sectors, %eax testl %eax, %eax jz clone_system_image_success cmpl $COPY_CHUNK_SECTORS, %eax jbe clone_system_image_use_remaining movw $COPY_CHUNK_SECTORS, dap_sector_count jmp clone_system_image_read_chunk clone_system_image_use_remaining: movw %ax, dap_sector_count clone_system_image_read_chunk: movl current_lba, %eax movl %eax, dap_lba movl current_lba + 4, %eax movl %eax, dap_lba + 4 movb boot_drive, %dl mov $dap, %si mov $0x4200, %ax int $0x13 jc clone_system_image_fail movl target_lba, %eax movl %eax, dap_lba movl target_lba + 4, %eax movl %eax, dap_lba + 4 movb selected_drive, %dl mov $dap, %si mov $0x4300, %ax int $0x13 jc clone_system_image_fail movw dap_sector_count, %ax addl %eax, current_lba adcl $0, current_lba + 4 addl %eax, target_lba adcl $0, target_lba + 4 subl %eax, remaining_sectors mov $'.', %al call print_char jmp clone_system_image_copy_loop clone_system_image_success: mov $crlf_text, %si call print_string clc jmp clone_system_image_done clone_system_image_fail: stc clone_system_image_done: pop %si pop %dx pop %cx pop %bx pop %ax ret apply_bootloader_to_target: push %ax push %bx push %cx push %dx push %si movb boot_drive, %dl call probe_drive jc apply_bootloader_fail movb selected_drive, %dl call probe_drive jc apply_bootloader_fail movl system_image_start_lba, %eax testl %eax, %eax jz apply_bootloader_fail movl %eax, current_lba movl $0, current_lba + 4 movl $0, target_lba movl $0, target_lba + 4 movl $BOOT_AREA_SECTORS, remaining_sectors apply_bootloader_copy_loop: movl remaining_sectors, %eax testl %eax, %eax jz apply_bootloader_success cmpl $COPY_CHUNK_SECTORS, %eax jbe apply_bootloader_use_remaining movw $COPY_CHUNK_SECTORS, dap_sector_count jmp apply_bootloader_read_chunk apply_bootloader_use_remaining: movw %ax, dap_sector_count apply_bootloader_read_chunk: movl current_lba, %eax movl %eax, dap_lba movl current_lba + 4, %eax movl %eax, dap_lba + 4 movb boot_drive, %dl mov $dap, %si mov $0x4200, %ax int $0x13 jc apply_bootloader_fail movl target_lba, %eax movl %eax, dap_lba movl target_lba + 4, %eax movl %eax, dap_lba + 4 movb selected_drive, %dl mov $dap, %si mov $0x4300, %ax int $0x13 jc apply_bootloader_fail movw dap_sector_count, %ax addl %eax, current_lba adcl $0, current_lba + 4 addl %eax, target_lba adcl $0, target_lba + 4 subl %eax, remaining_sectors jmp apply_bootloader_copy_loop apply_bootloader_success: clc jmp apply_bootloader_done apply_bootloader_fail: stc apply_bootloader_done: pop %si pop %dx pop %cx pop %bx pop %ax ret reboot_system: movw $0x1234, 0x472 ljmp $0xFFFF, $0x0000 halt_system: cli .halt: hlt jmp .halt title_text: .asciz "OS8 DOS System Installer\r\n" subtitle_text: .asciz "16-bit real mode fallback path\r\nDeploys the bundled regular OS system image to another BIOS disk\r\n\r\n" menu_text: .asciz " 1.
